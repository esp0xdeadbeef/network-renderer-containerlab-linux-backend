# Regression state

- state=solved | target=containerlab-static-client-reservation-capability-gate | evidence=2026-06-02 the Containerlab/Linux backend detects DHCP reservation contracts on selected CLAB runtime targets and fails closed before artifact emission with a diagnostic naming the unsupported reservation surface. The full `s-router-nixos` loop later proved the first implementation boundary was too broad: the check ran before deployment-host filtering and rejected NixOS-only reservation target `runtimeTargets.esp-nixos-router-access-client.advertisements` while rendering `s-router-clab`. The check now runs after target-host filtering, so selected CLAB targets still fail closed while unrelated NixOS targets do not poison CLAB render. Focused proofs passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dhcp-reservation-contract-fail-closed.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-deployment-host-filter.sh`. Trace retagged to `FS-970-HDS-010-SDS-010-SMS-010`. | expected=CLAB renderer detects `runtimeTargets.*.advertisements.*.reservations` on the runtime targets it will materialize and either materializes a supported target contract or emits a concise unsupported-feature failure before SAT source promotion. Runtime targets filtered out by explicit deployment host selection must not be treated as CLAB-rendered surfaces. | owner=network-renderer-containerlab-linux-backend | reason=Containerlab materializes explicit CPM output only for selected CLAB runtime targets. A target limitation must be explicit and fail closed, but it must not reject unrelated renderer targets that are removed by the deployment-host filter.

- state=solved | target=clab-tooling-image-cache-update-cycle | evidence=2026-05-31 HAT found `clab-frr-plus-tooling:latest` and persisted cache artifacts stale in `s-router-clab-container`; the image missed required debug tools and the builder asserted `staticd=yes` even though the base FRR image states staticd daemons are always started. `docker-clab-frr-plus-tooling/build.sh` now loads a keyed cache tar before build, builds with `--pull=false`, verifies required commands plus FRR `bgpd` and `staticd` readiness inside the image before saving cache, and `docker-clab-frr-plus-tooling/update-repo-cache-with-vm.sh` refreshes repository or user-supplied cache paths from a Docker-capable NixOS-shell VM while refusing `/nix/store` output paths. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-vm-docker-readiness.sh` passes. Live HAT rebuilt and cache-loaded Dockerfile key `fb78cfcdda54a5b4f3aac55d5627c0dcde00a0c4d70d02946cf8dbaa8c8a1d23`, and offline VM HAT wrote verified cache artifacts to both the repository `.cache` path and a user-supplied fallback path. | reason=The Containerlab/Linux renderer owns the tooling image builder and cache update module. The NixOS `s-router-clab` deploy path owns invoking that builder before `containerlab deploy`; live deploy-path HAT remains tracked in `network-codex-agent` until a clean deploy executes it.

- state=solved | target=upstream-selector-return-lane-table-preservation | evidence=2026-05-24 full-loop live CLAB failed `clab-client01 ping 1.1.1.1` even though `clab-router-policy ip route get 1.1.1.1 from 10.50.20.100 iif eth2` selected the client WAN lane. Live `clab-router-upstream` had table `1004` for the client policy lane, but that table contained `10.50.20.0/24 via 10.50.0.14 dev eth1` and return lookups from core ingress selected the core-facing path instead of `via 10.50.0.32 dev eth4`. A temporary live replacement of only that table route plus an iif rule for the core side made client IPv4 egress pass. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-no-main-defaults.sh` now asserts a lane table keeps the lane interface's own non-default return route for the tenant prefix and also installs source-interface rules for both policy-side and matching core-side ingress. | reason=The CLAB renderer owns Linux RPDB/table materialization from explicit CPM lane/route facts. It must not make one case work by adding a broad main/default route, and it must not let a duplicate-looking core-side policy route override the policy lane's own return route inside that lane table.

- state=solved | target=segmented-policy-routing-without-main-default | evidence=2026-05-23 focused renderer checks prove CLAB does not paper over segmented policy routing with broad main-table defaults: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-linux-policy-rule-shell-safety.sh` proves policy-only IPv4/IPv6 defaults are emitted as `ip route replace table <lane>` plus `ip rule add iif <tenant-if>`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-rendering.sh` proves the core overlay underlay default is preserved only where CPM selected the access path, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-no-main-defaults.sh` proves policy-only defaults do not appear in `_render_default_routes`. Primary references kept for the contract: Linux `ip-rule(8)` documents RPDB selectors/actions and that `main` is only the normal non-policy table (`https://man7.org/linux/man-pages/man8/ip-rule.8.html`); nftables docs state prerouting/forward/output hooks and packet/conntrack marks (`https://wiki.nftables.org/wiki-nftables/index.php/Configuring_chains`, `https://wiki.nftables.org/wiki-nftables/index.php/Setting_packet_metainformation`). | reason=CLAB materializes explicit CPM policy-routing intent with Linux RPDB and nftables. Endpoint validation must use source/ingress route lookups and live packets; router-self `ping 1.1.1.1` from `role=policy` remains intentionally non-authoritative unless CPM emits a router-self/main-table route.

- state=solved | target=nebula-provider-boundary-clab-reachability-materialization | evidence=2026-05-22 focused renderer test `tests/test-overlay-underlay-access-rendering.sh` feeds a synthetic CPM where an overlay core gets its default route from the selected access node and also has an upstream p2p with no default. The test proves CLAB renders the access-client<->core-nebula p2p bridge, preserves the core default via access, does not add a default route on the selected access side, and does not invent a default via the unrelated upstream link. A current local s-sigma render from `/tmp/sigma-cpm-final.json` also contains the modeled p2p bridge; that same CPM still includes an extra core-nebula default via upstream, which the renderer correctly preserves rather than invents. | reason=CLAB owns CLAB/Linux realization, but only as materialization of explicit CPM/provider-neutral data. Extra or missing default routes in rendered CLAB output must be fixed in the CPM/NFM route contract unless they are absent from CPM and created here.

- state=solved | target=clab-tooling-image-debug-tools | evidence=2026-05-22 `docker-clab-frr-plus-tooling/Dockerfile` installs `ripgrep`, `nmap`, and nft/debug tooling, and `tests/test-vm-docker-readiness.sh` gates the image and VM startup expectations. | reason=The Containerlab/Linux renderer owns the `clab-frr-plus-tooling` image used by generated Linux routers; debug tooling required to validate rendered routes, nftables, DNS, and exposure belongs in that image, not in model semantics or local host glue.

- state=solved | target=clab-modeled-multipath-route-installation | evidence=2026-05-15 current s-router-clab regression reported live `FAIL routes expected=2541 missing=288` beginning with routes via `10.50.0.31`; generated CLAB YAML emitted repeated `ip route replace <dst> via <a>` and `ip route replace <dst> via <b>` for the same CPM destination, so Linux kept only the last next-hop. `clabgen/s88/CM/linux_routes.py` now groups duplicate explicit CPM next-hops into one `ip route replace <dst> nexthop ... nexthop ...` command for static and default IPv4/IPv6 routes. | reason=The renderer bug was the destructive materialization of explicit CPM multi-next-hop route data; the full lab loop remains the integration gate for live kernel route exposure.

- state=solved | target=vm-example-stale-fabric-container-cleanup | evidence=2026-05-15 full-lab VM worker 5 failed during the second example in the same worker with repeated `the 'fabric' lab has already been deployed`; `run-in-vm.sh` now removes all matching `clab-fabric-*` containers with `docker ps -aq --filter 'name=^clab-fabric-' | xargs -r docker rm -f` after Containerlab metadata cleanup, and `tests/test-vm-example-lab-cleanup.sh` gates that exact fallback. | reason=The renderer VM example harness reuses a lab name across examples, so helper cleanup must remove stopped as well as running stale fabric containers before deploying the next explicit rendered topology.
