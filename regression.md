# Regression Log

- state=solved | target=FS-960-HDS-010-SDS-016-SMS-010 direct-host CLAB lifecycle prerequisite | first-bad-artifact=the exact pushed three-host cold stage `FS-230-HDS-010-SDS-010-SMS-040-retry-3` booted all guests with new systems and exact pins, but `s-router-clab-render-live.service` failed closed because `containerlab deploy --reconfigure` attempted to remove prior lab entries from `/etc/hosts` and the renderer-created NixOS entry was a read-only store symlink; the earlier construction test proved only existence and therefore produced false-positive readiness | evidence=`tests/test-fs960-hds010-sds016-sms010-clab-autostart.sh` now evaluates the real NixOS host module and requires a copied `0644` hosts artifact, then independently rejects both a missing entry and a read-only symlink with distinct diagnostics; SMS-060 preserves fail-closed handling of every Containerlab `ERRO` and separately classifies the non-writable prerequisite; the full renderer suite and flake checks are the construction gates | reason=`/etc/hosts` mutability is a direct-host Containerlab lifecycle prerequisite owned by this renderer, not topology, intent, inventory, or a runtime hotpatch. The renderer now materializes the NixOS hosts content as a copied writable file, asserts that effective mode, and checks the runtime artifact before deployment. A fresh pushed three-host cold stage remains the separate SMS acceptance gate.

- state=solved | target=FS-800-HDS-030-SDS-020-SMS-020 CLAB PPPoE DHCPv6-PD runtime safety | first-bad-artifact=the full renderer suite found that the new PD supervisor silently discarded every `dhcpcd` exit through `|| true` and added its link-local reply allow to nonexistent `inet router`, while the renderer's WAN input chain is `inet filter input` | evidence=`tests/test-fs800-hds030-sds020-sms020-pppoe-ipv6-prefix-delegation.sh` requires an explicit retry diagnostic, rejects the silent fallback, and requires the reply rule on the already materialized WAN input table; the FS-310 fallback and nftables primitive audits remain green | reason=the supervisor may retry after a reported client exit but may not silently hide it, and the reply exception must use the renderer-owned WAN firewall surface rather than inventing an unmaterialized table. Fresh pushed cold-stage evidence remains required.

- state=solved | target=FS-310-HDS-030-SDS-010-SMS-112 tenant interface mapping fail-closed contract | first-bad-artifact=the complete renderer suite found a stale acknowledged `network or ""` default in `interface_model.py`; an absent network was converted into an empty lookup key before the function eventually failed | evidence=`tests/test-fs310-hds030-sds010-sms112-clab-fail-closed-contract.sh` no longer carries that known-gap waiver and passes its active empty-default seeded negative | reason=the interface mapper now skips absent address networks explicitly and performs tenant lookup only with a valid normalized network; no empty-string identity enters the lookup path.

- state=solved | target=FS-230-HDS-010-SDS-010-SMS-040 protected IPv6 public-ingress forwarding validation on CLAB/Linux | first-bad-artifact=the complete five-node `network-labs` row compiled through compiler, forwarding model, and CPM, but `generate-clab-config` rejected the policy node with `missing-tenant-accept ... upstream->downstream`; CPM carried exactly one protected runtime IPv6 UDP/4242 accept with no translation and the CLAB firewall renderer emitted that rule through a runtime materializer shell command | evidence=`tests/test-fs230-hds010-sds010-sms040-nebula-ipv6-public-ingress.sh` makes the runtime-materializer form pass the FS-370 completeness validator and still rejects a missing accept; the complete row renders five nodes and five protected UDP/4242 hops with no TCP/4242 or NAT/masquerade output | reason=the FS-370 validator now recognizes exact interface operands in the static JSON-quoted and runtime shell-quoted command forms, requires ingress, egress, and `counter accept` in the same command, and therefore no longer mistakes safe shell serialization for a missing rule; a pushed cold stage remains required

- state=solved | target=FS-970-HDS-010-SDS-020-SMS-040 mode-less DNS proxy lifecycle classification on CLAB/Linux | first-bad-artifact=the pushed exact-source enrollment cold stage `/tmp/s-router-stage-FS-970-HDS-010-SDS-020-SMS-040-enrollment1` booted all three isolated hosts with new systems and zero failed units before CLAB deployment; the `client-edge` container materialized and ran `/tmp/clabgen-dns-proxy.py`, but the renderer labeled it `clab.dns.runtime=unbound` without emitting `/tmp/clabgen-reconcile-unbound.sh`, so the host lifecycle gate failed closed with `advertises DNS reconciliation without a runtime script` | evidence=`tests/test-fs970-hds010-sds020-sms040-protected-reservation-runtime.sh` now requires the mode-less DNS service to materialize its proxy while carrying no Unbound lifecycle label or reconciliation script; the explicit-recursion FS-540 authority test still requires and receives the Unbound label and passes | reason=the lifecycle label now uses the same explicit recursion-authority predicate as `render_dns_service`, so the host reconciles only actual Unbound runtimes while leaving the legacy proxy lifecycle unchanged; a new pushed cold stage remains required

- state=solved | target=FS-270-HDS-010-SDS-010-SMS-020 dual-stack ICMP policy materialization on CLAB/Linux | first-bad-artifact=the exact pushed cold stage `/tmp/s-router-stage-FS-270-HDS-010-SDS-010-SMS-020-policy-state-owner` booted all three isolated hosts and all six CLAB nodes, but `s-router-clab-render-live.service` failed closed during `access-destination` bundle 2; bundle 1 published its PID-scoped marker and the IPv4 forward rule was installed, while the immediately following emitted IPv6 rule `ip6 saddr <prefix> icmpv6 counter accept` was rejected before the bundle-2 marker, `clab_guard`, and resolver materialization | evidence=`tests/test-fs270-hds010-sds010-sms020-relation-policy-state-route-selection.sh` drives the pushed row through CPM and CLAB, extracts all four relation-owned policy statements from the generated exec bundles, requires exact family-specific protocol selectors, parses the complete rule batch with real `nft -c` in an isolated user/network namespace, and rejects an IPv6 protocol match assigned to an IPv4 rule; the adjacent FS-230 stateful-return, FS-370 nft literal-boundary, and FS-960 runtime-syntax tests pass | reason=CPM explicitly supplies separate `family=ipv4, proto=icmp` and `family=ipv6, proto=icmpv6` matches; the renderer now serializes those protocol values as `meta l4proto icmp` and `meta l4proto icmpv6` and fails closed on an explicit family/protocol conflict. A new pushed cold stage remains required before downstream live acceptance.

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 controlled-authority readiness and service input | first-bad-artifact=successive exact cold stages found that the CLAB authority launcher returned before Knot persisted, that the default PID and control-socket paths under `/run/knot` were unusable, and that three harness-owned IPv6 addresses remained tentative. Once authority readiness passed, the canonical live validator showed the first recursive query reaching the access and core interfaces while the base input chain dropped it because no DNS service input rule had been materialized, even though the CPM allowFrom prefix covered the client and loopback recursion worked | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-540-HDS-010-SDS-010-SMS-045-controlled-authority.sh` requires deterministic synthetic IPv6 assignment, address readiness, writable Knot runtime paths, persistent process/listener readiness, and idempotent dual-stack UDP/TCP input rules for every CPM allowFrom and requester-policy prefix on core, recursive, and local services. `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` passes 57/57 | reason=the controlled provider now reports ready only when the authority remains usable, and explicit CPM service requesters can reach Unbound through the existing fail-closed input chain while all unmodeled sources remain dropped; new pushed cold-stage evidence remains required

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 deterministic local-only refusal on CLAB/Linux | first-bad-artifact=source inspection showed CLAB shared NixOS's original `local-zone: "." static` projection; the first construction fix also shared the overly broad `refuse_non_local` default that the second exact NixOS cold stage proved refuses a permitted forwarded namespace | evidence=`NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` requires ordinary local-only clients to remain `allow`, relation-scoped lateral requesters to remain `refuse_non_local`, and the terminal catch-all to be `local-zone: "." refuse` rather than `static`, and passes. `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=8 bash tests/test.sh` passes 57/57 | reason=CLAB now materializes the same namespace-aware CPM miss action as NixOS: more-specific local/forwarded namespaces remain usable and every other name terminates REFUSED without recursion or fallback; new pushed cold-stage evidence remains required

- state=solved | target=FS-270-HDS-010-SDS-010-SMS-020 relation-scoped policy-state route selection on CLAB/Linux | first-bad-artifact=the pinned CPM emitted four portable selectors for forward/return and IPv4/IPv6 plus exact bounded routes in selected policy tables, but the CPM-to-solver adapter discarded `effectiveRuntimeRealization.routeSelectionRules`; the focused full-pipeline test was red with `selectors=4 rules=0 routes=4` | evidence=`tests/test-fs270-hds010-sds010-sms020-relation-policy-state-route-selection.sh` now drives the pushed lab fixture through CPM and CLAB, requires all four exact source+destination+ingress rules and bounded routes, and rejects wrong-table, missing-ingress, missing-return, missing-route, and transitive-default seeded negatives | reason=CLAB now preserves the explicit CPM selector contract through its node model and emits it before generic policy rules, while failing closed instead of deriving routing or authority locally; cold staged NixOS/CLAB live evidence remains required for downstream acceptance

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 dual-stack local DNS return path on CLAB/Linux | evidence=`tests/test-fs540-hds010-sds010-sms045-local-dns-return-path.sh` supplies the compressed local-dns service identity alongside an equivalent expanded IPv6 interface address and requires dual-stack locally originated source rules without ingress-interface context plus both return-subnet routes | reason=the CLAB renderer already compares service addresses semantically with `ipaddress`, so the NixOS text-form regression was not present here; the explicit parity gate prevents future divergence and a new pushed cold stage remains required

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 isolated provider bridge membership on CLAB/Linux | first-bad-artifact=the 2026-07-18 exact-source cold stage rendered `clab.link.bridge: isp-primary`, but the core `wan0` peer was `host:veth-*` and was not a member of the selected bridge, so provider-side RA could not reach core | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-045-controlled-authority.sh` was red on the exact emitted link and now requires both core `wan0`/selected and `wan1`/alternate links to terminate on their realized bridge node without a `host:` fallback; the selected authority sidecar remains the only provider sidecar | reason=one-ended CPM links with explicit `hostUplink.mode = isolated` now materialize their modeled bridge endpoint directly, while native, VLAN, NAT, and generic host-link behavior is unchanged | live-boundary=a pushed exact-source cold stage must still prove real runtime L2 membership and first-boot selected IPv6 address/default acquisition on CLAB

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 autonomous provider SLAAC on CLAB/Linux | first-bad-artifact=the 2026-07-18 exact-source cold stage carried `ipv6.method = "slaac"` through the pushed lab inventory and CPM, but the CLAB authority sidecar rendered dnsmasq `ra-only`; like NixOS, the CLAB core had no global IPv6 address or selected-table default | evidence=the network-labs SMS now treats RA without the autonomous A-bit as a seeded negative, and `tests/FS-540-HDS-010-SDS-010-SMS-045-controlled-authority.sh` was red before repair and now requires `ra-only,slaac,64` in the generated sidecar | reason=the renderer now preserves the modeled autonomous-SLAAC contract while retaining RA-only DHCP suppression | live-boundary=a new pushed exact-source cold stage must pass the dual-stack NixOS/CLAB protocol before the SMS can become OK

- state=solved | target=FS-540-HDS-010-SDS-010-SMS-045 controlled iterative authority on CLAB/Linux | first-bad-artifact=the pushed lab source and CPM carried an isolated dual-stack root/delegation/terminal authority, but the CLAB DNS and lab-emulation renderers ignored `services.dns.validationAuthority`; generated core Unbound still used public root data and no selected-provider DHCP/RA/authority runtime existed | evidence=`tests/FS-540-HDS-010-SDS-010-SMS-045-controlled-authority.sh` compiles the pushed source through CPM and CLAB, requires one selected-provider-only sidecar with DNS-disabled DHCPv4, IPv6 RA/SLAAC, real root/delegation zones, controlled Unbound root hints, no public trust oracle, and privacy-safe rejection of seeded authority/egress divergence; the existing FS-540 reproducibility and FS-960 lab-emulation capability gates remain green | reason=the renderer now derives the harness artifact only from explicit CPM `services.dns.validationAuthority`, validates it against the model-owned selected DNS egress, and leaves the alternate isolated provider unable to answer. Cold NixOS/CLAB live evidence remains required before downstream SIT acceptance.

- state=solved | target=FS-525/FS-540 deterministic iterative DNS egress on CLAB/Linux | first-bad-artifact=successive cold stages exposed three replay boundaries after CPM selection: neither selected-table default was first maintained; a braced shell expression was then lost at deploy evaluation; finally the RA dump contained a non-replayable human expiry and omitted the selected device because the query itself filtered by that device, leaving IPv6 table 1002 empty | evidence=`tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` drives the pushed lab row through CPM, solver, node model, selected-interface dynamic acquisition, and DNS emission; it requires DHCP and RA lifecycle handling for the exact table, validates the final shell payload, rejects the evaluation-sensitive braced expansion, seeds the human-readable expiry, and requires the exact CPM runtime interface on replay | reason=the renderer normalizes the observed RA route, removes only its non-replayable lifetime, and explicitly restores CPM's selected runtime interface while preserving gateway, protocol, metric, and preference; it allocates no route ID, address, interface, or uplink. A new pushed cold-stage remains required for downstream SIT acceptance.

- state=solved | target=FS-525/FS-540 reproducible DNS authority materialization | first-bad-artifact=`dns_authority.py` accepted named-core resolvers without terminal `endpointAuthority`, ignored provider `serviceEndpointBindings`, and allowed a non-transparent local zone to shadow the same modeled forward namespace | evidence=`tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` compiles the pushed isolated CLAB inventory through CPM, proves access forwarders and core listeners share the exact relation/terminal authority, renders bilateral source-scoped `refuse_non_local` and forwarding-compatible local zones with Unbound, validates all three generated files with `unbound-checkconf`, and rejects seeded authority leak, endpoint/listener divergence, namespace shadowing, renderer divergence, and fatal warnings without logging addresses | reason=explicit CPM recursion modes use the same Unbound semantics as NixOS and now fail with `DNS_RENDERER_CONTRACT_DIVERGENCE` or `DNS_LOCAL_NAMESPACE_SHADOWED` when the service would not be reproducible; mode-less legacy fixtures retain the proxy until migrated. Cold-staged NixOS/CLAB parity remains a downstream SIT acceptance condition.

- state=solved | target=FS-370-HDS-010-SDS-010-SMS-110 CLAB/Linux nftables forwarding materialization, exercised by FS-260-HDS-010-SDS-010-SMS-010 | evidence=after a pushed cold stage, `s-router-clab-render-live.service` failed in `containerlab-deploy`; the isolated upstream-selector had all route/rule commands preceding its firewall materialization but no modeled forwarding rules or `clab_guard` table, and a read-only `nft -c` reproduction in that container rejected the emitted shell form with `syntax error, unexpected policy`; `tests/test-fs370-hds010-sds010-sms110-clab-forwarding-materialization.sh` now executes the generated command across a real shell argument boundary, proves nft receives one complete grammar argument with quoted `core`/`policy` string literals, and actively rejects the former multi-argument shell form | reason=`clabgen/s88/CM/cpm_firewall_rules.py` now quotes the complete nft grammar statement after serializing interface names and comments as nft string literals, so shell parsing cannot reinterpret a modeled runtime interface as an nftables keyword.

- state=solved | target=FS-230-HDS-010-SDS-010-SMS-030 stateful-return materialization in the Containerlab/Linux renderer, exercised by FS-260-HDS-010-SDS-010-SMS-010 | evidence=`tests/test-fs230-hds010-sds010-sms030-stateful-return-rendering.sh` proves `connectionState=established,related` becomes an nftables `ct state established,related` match and that a return rule with missing or unsupported state fails closed; the isolated FS-260 NixOS/CLAB row exposed the previous blanket reverse accept | reason=`clabgen/s88/CM/cpm_firewall_rules.py` preserved interfaces, relation identity, matches, and action but discarded CPM `connectionState`/`returnRule`, widening stateful return into reverse new-flow authority.

- state=solved | target=FS-970-HDS-010-SDS-020-SMS-040 protected dual-stack reservation materialization on CLAB | evidence=the isolated VLAN398 cold-stage run from pushed renderer `8f305ba4817bc28066a98ee71ca416448a40776b` proved that node-init could bind Kea to an interface instance later replaced by Containerlab; `tests/test-fs970-hds010-sds020-sms040-protected-reservation-runtime.sh` now proves node-init emits runtime-only reconcile scripts and a scoped lifecycle label, while `tests/test-fs380-hds020-sds010-sms050-host-bridge-netfilter.sh` proves the host executes reconciliation after deploy and before runtime verification | reason=the owning renderer no longer treats node-init PID state as readiness: it materializes secret-backed configuration during init, starts both Kea families only after Containerlab has finalized links, and fails deployment unless UDP 67 and 547 become ready. Live NixOS/CLAB integration acceptance remains owned by the downstream SDS/SIT stage.
- state=solved | target=FS-540 core DNS service endpoint materialization on CLAB/Linux | first-bad-artifact=a cold-staged `s-router-clab` topology carried the exact dual-stack CPM `serviceEndpointBindings` and terminal attachment into the DNS configuration, but the renderer discarded each interface `backingRef` and started its readiness wait without proving the bound service addresses belonged to that terminal runtime interface; both access resolvers had stable listeners while core correctly failed closed with no Unbound process | evidence=`tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` requires the explicit terminal attachment to resolve to exactly one preserved CPM-backed runtime interface, preserves an already materialized terminal address, emits an explicitly distinct service address before Unbound readiness, and rejects an unbound terminal without exposing address material; cold-stage evidence is `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage6` | reason=the renderer now preserves `backingRef` through CPM-to-solver and node-model conversion, then uses only the exact CPM terminal interface and service endpoint addresses; an address absent from that interface is added as a host prefix, while an already materialized p2p address is left intact. No interface, address, or attachment is inferred locally. A fresh pushed cold stage remains required for downstream SIT acceptance.
- state=solved | target=FS-540 ordered CLAB node initialization | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage7` proved Containerlab launched a node's bundled `exec` entries concurrently: the later DNS bundle created core's explicit endpoint while the earlier addressing bundle could subsequently replace that interface state, leaving the IPv6 listener absent and the resolver fail-closed | evidence=`tests/test-fs320-hds010-sds010-sms030-runtime-interface-mapping.sh` seeds 205 primitives across three bundles and requires every later bundle to wait for its predecessor's container-generation-scoped completion marker before executing; the complete 54-test renderer suite remains green | reason=each bundle now derives the current container generation from `/proc/1/stat`, waits up to the bounded fail-closed deadline for the preceding bundle marker, and publishes its own marker only after all primitives succeed under `set -e`; this preserves renderer order without collapsing the bounded Containerlab exec payloads. A fresh pushed cold stage remains required for downstream SIT acceptance.
- state=solved | target=FS-540 non-destructive dual-stack DNS listener readiness | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage8` proved the core service binding reused the exact p2p interface addresses; assigning the same IPv6 address again as `/128` restarted/replaced its p2p state and left the listener absent, while a ten-second DAD/readiness window was also too short for one access node | evidence=`tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` proves existing terminal addresses are not re-addressed, explicitly distinct CPM service endpoints still receive `/32` or `/128`, and listener and resolver-process convergence are independently bounded | reason=service endpoint materialization is now idempotent against explicit terminal interface addressing and readiness waits for stable, non-tentative dual-stack addresses before starting Unbound; it remains fail-closed when that state does not converge. A fresh pushed cold stage remains required for downstream SIT acceptance.
- state=solved | target=FS-540 cold-stage dual-stack DNS listener convergence on CLAB/Linux | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage9` used pushed renderer `a4407fd23f4974c763017a66b617f4ea60173549`; `access-recursive` completed its first ordered bundle and wrote a valid Unbound configuration in its second bundle, but no pid/log was created because at least one configured IPv6 listener did not become non-tentative within the renderer's sixty-second listener window; after topology convergence the same four address predicates were all true | evidence=the cold stage proved new boot/system generations and exact source hashes on all three isolated lab hosts, NixOS and test-clients had zero failed units, CLAB failed closed with zero published lab nodes, the first-bundle generation marker existed, `unbound-checkconf` passed, all four redacted listener-presence predicates passed after convergence, and no runtime hotpatch was used; `tests/test-fs540-hds010-sds010-sms035-reproducible-dns-authority.sh` now requires one bounded 300-second listener convergence loop and preserves the separate 60-second process/socket loop | reason=the renderer now tolerates full cold Containerlab address convergence without weakening exact-address or non-tentative predicates; it still fails closed with redacted diagnostics when listener state or the subsequently started resolver does not converge. A fresh pushed cold stage remains required for downstream live acceptance.
- state=solved | target=FS-540 CLAB DNS post-deploy lifecycle ordering | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage10` used pushed renderer `f224ed130b419cf39b4846390cbec8eb9a25e009`; `access-local` completed ordered address materialization, but its IPv6 LAN listener remained tentative because `lan0` was `NO-CARRIER` until host bridge rematerialization after `containerlab deploy`, while the in-node Unbound readiness loop blocked deploy and hit the host's 180-second idle guard | evidence=the exact listener remained present-but-tentative on `lan0`, with operstate value `lowerlayerdown`, carrier value `0`, `accept_dad=1`, and no Unbound pid; NixOS and test-client profiles cold-booted with zero failed units, all source hashes matched, CLAB failed closed, and no runtime hotpatch was used; the focused FS-540 test now proves config/policy materialization remains in node-init while exact non-tentative listener and socket readiness runs through a DNS lifecycle label after bridge rematerialization in both deploy entrypoints, and the adjacent FS-380, FS-960, and FS-970 lifecycle tests pass | reason=Unbound configuration and CPM policy materialization remain ordered node-init construction, but process start and listener readiness are now reconciled only after Containerlab completes and the host rematerializes bridge links, matching the existing post-deploy lifecycle boundary used for Kea. A fresh pushed cold stage remains required for downstream live acceptance.
- state=solved | target=FS-540 CLAB DNS readiness for point-to-point service addresses | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage11` used pushed renderer `6894f55bcd052b51f4acb2713c538402aba80072`; both access resolvers reconciled after bridge setup, while core failed its bounded listener predicate even though both modeled addresses were present and non-tentative on the exact terminal interface | evidence=the core terminal is a point-to-point interface, so `ip -o address show` emits its local address as a bare field followed by `peer`, whereas the previous rendered readiness predicate accepted only a local field containing `address/prefix`; the host service failed closed after the 300-second bound, all six containers remained present, NixOS and test-clients had zero failed units, exact source hashes and new boot/system generations matched, and no runtime hotpatch was used; the focused FS-540 construction test was made red before the repair and now requires the P2P local-field form | reason=the exact-address predicate now accepts either the ordinary `address/prefix` local field or the point-to-point bare-local field while retaining the same non-tentative and no-DAD-failure requirement. A fresh pushed cold stage remains required for downstream live acceptance.
- state=solved | target=FS-540 direction-scoped recursive DNS egress on CLAB/Linux | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage12` reached six healthy CLAB containers, but the NixOS half of the shared live row proved the cross-renderer contract defect before CLAB query acceptance: both renderers add the same process-wide Unbound UID policy rule absent from CPM | evidence=CPM supplies only selected interface, table, priority, and firewall mark; `clabgen/s88/CM/dns_service.py` independently added dual-stack `uidrange` rules, while the destination-port mark already distinguishes resolver-origin upstream requests from source-port replies. The focused FS-540 test was made red before repair and now rejects both UID lookup and `uidrange` emission while preserving dual-stack CPM fwmark rules. This finding is representative of the FS-540 isolated row; the full example set has not been live-audited for this runtime identity defect | reason=the renderer now projects only CPM's upstream DNS-request mark and no longer invents process-wide provider routing, so internal replies retain normal relationship routing. A fresh pushed cold stage remains required for downstream live acceptance.
- state=solved | target=FS-540 pre-socket recursive DNS route selection on CLAB/Linux | first-bad-artifact=cold stage `/tmp/s-router-stage-FS-540-HDS-010-SDS-010-SMS-045-restage13` proved the equivalent output-mark-only NixOS realization fails before a DNS packet exists; the CLAB renderer emitted the same late nft output mark plus fwmark rule and no pre-socket DNS selector | evidence=the revised pushed SDS/SMS makes this a cross-renderer construction predicate: the resolver identity combined with UDP/TCP destination port 53 must select the CPM table during initial socket lookup, while a reply to an internal ephemeral port must not match. The focused CLAB test was red before repair and now requires four scoped rules and rejects any process-wide UID add rule | reason=CLAB now renders `uidrange` plus `ipproto` plus `dport 53` rules for both families and transports, without restoring the former process-wide UID rule. Fresh pushed cold-stage evidence remains required for downstream acceptance.
